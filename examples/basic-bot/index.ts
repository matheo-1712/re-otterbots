/**
 * Bot d'exemple — point d'entrée lancé par `npm run example`.
 *
 * Il consomme le moteur via son nom de package (`re-otterbots`), résolu vers
 * ../../src par examples/tsconfig.json : ce que tu écris ici est exactement ce
 * qu'écrira un utilisateur du package publié.
 */
