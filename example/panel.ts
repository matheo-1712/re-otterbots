// Dans un vrai projet : import { runWebPanel } from 're-otterbots';
import { runWebPanel } from '../src';

// Interface web seule, sans bot : pilote les bots distants listés dans panel.yml (npm run panel)
runWebPanel();
